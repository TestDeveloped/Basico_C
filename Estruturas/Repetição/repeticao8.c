/*Faça um programa em C que exibe somente os números pares
existentes no intervalo de x (inclusive) a y (inclusive). Os valores
de x e de y devem ser fornecidos pelo usuário.
Nota: não esqueça de verificar se o intervalo fornecido pelo
usuário é válido, isto é, o valor de x deve ser menor que o valor de
y. Caso contrário, exiba uma mensagem informando que o
intervalo é inválido. */